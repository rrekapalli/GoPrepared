---
name: goprepared-flutter-feature
description: Add or edit GoPrepared Flutter screens. Use when working on go-prepared-app UI, routing, or Riverpod state.
---

# Flutter Feature Skill

1. Read `Docs/ui-screen-map.md`
2. Use `go_router` + Riverpod patterns in `lib/features/`
3. API via `journey_repository.dart`
4. Verify: `flutter analyze` && `flutter test`
