# Sync note: Python models in go-prepared-content/goprepared_content/schemas/ai/
# Java models in go-prepared-api/.../ai/dto/AiContracts.java
# Flutter models in go-prepared-app/lib/data/models/ai_models.dart
# Update contracts/ai/*.schema.json first, then sync all three.

Write-Host "Contract schemas live in contracts/ai/ — sync bindings manually or extend this script."
